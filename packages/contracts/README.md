# Shared contracts

This directory is the single boundary for shared cross-language contracts.
Python Engine-only representations belong in engine/src/footcap_engine/contracts and must not duplicate these contracts.
