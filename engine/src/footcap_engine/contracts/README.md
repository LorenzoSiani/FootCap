# Internal Python representations

This directory is reserved for Python Engine-only types and models.
It must not define or duplicate shared cross-language contracts; those belong in packages/contracts.
